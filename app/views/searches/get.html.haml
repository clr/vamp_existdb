#page.container_12
  #header.grid_12
    XQuery VAMP Archive
  #search.grid_12
    %form{:action => '/search', :method => :post}
      %textarea{:name => 'qu', :style => 'width:80em;height:30em'}
        = default_search
      %button{:type => 'submit'}
        search
  .clear
  #content
    - if @resource
      Result: 
      = link_to search_url(@resource, :format => :xml), search_url(@resource, :format => :xml)
      %textarea{:name => 'qu', :style => 'width:80em;height:30em'}
        =raw @resource.result
      .clear
